# Deps for aii-openrouter-llms skill.
# Usage: uv venv .venv --python=3.12 && uv pip install -r server_requirements.txt
# Also requires: uv pip install -e <project_root>/aii_lib
httpx>=0.27.0
requests>=2.31.0
pyyaml
python-dotenv
