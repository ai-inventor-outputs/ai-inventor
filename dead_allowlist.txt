# dead_allowlist.txt — symbols dead can't see as used.
# One symbol per line. Lines starting with '#' or blank are ignored.
#
# Each section below paired with the WHY (framework-required pattern).
# When deleting source, also delete its name here, otherwise we silently
# mask future genuine dead-code finds.
#
# Names NOT in this file but flagged by `dead` are real dead code candidates
# (kept out so they keep flagging until either silenced or deleted).

# ── django_settings ── Django settings module — read at runtime via `settings.X` from inside Django ──
ACCOUNT_EMAIL_VERIFICATION
ACCOUNT_LOGIN_BY_CODE_ENABLED
ACCOUNT_LOGIN_METHODS
ACCOUNT_SIGNUP_FIELDS
AII_PIPELINE_DIR
AII_POD_HEARTBEAT_INTERVAL_SECONDS
AII_POD_LIVENESS_CACHE_SECONDS
AII_POD_ORCH_DATA_CENTER
AII_POD_ORCH_IMAGE
AII_POD_ORCH_TEMPLATE_ID
AII_POD_ORCH_VOLUME_ID
AII_SITE_DOMAIN
AII_SITE_NAME
AII_SSE_HEARTBEAT_SECONDS
ALLOWED_HOSTS
APPEND_SLASH
ASGI_APPLICATION
AUTHENTICATION_BACKENDS
AUTH_PASSWORD_VALIDATORS
CORS_ALLOWED_ORIGINS
CORS_ALLOW_ALL_ORIGINS
CORS_ALLOW_CREDENTIALS
CSRF_COOKIE_SAMESITE
CSRF_COOKIE_SECURE
CSRF_TRUSTED_ORIGINS
DATABASES
DATA_UPLOAD_MAX_MEMORY_SIZE
DEFAULT_AUTO_FIELD
EMAIL_BACKEND
EMAIL_HOST
EMAIL_HOST_PASSWORD
EMAIL_HOST_USER
EMAIL_PORT
EMAIL_USE_TLS
FORCE_SCRIPT_NAME
HEADLESS_FRONTEND_URLS
HEADLESS_ONLY
INSTALLED_APPS
INTERNAL_AUTH
LANGUAGE_CODE
LOGGING
LOGIN_REDIRECT_URL
LOGIN_URL
LOGOUT_REDIRECT_URL
MIDDLEWARE
NINJA_DEFAULT_THROTTLE_RATES
PIPELINE_URL
ROOT_URLCONF
SECRET_KEY
SECURE_CONTENT_TYPE_NOSNIFF
SECURE_HSTS_INCLUDE_SUBDOMAINS
SECURE_HSTS_PRELOAD
SECURE_HSTS_SECONDS
SECURE_PROXY_SSL_HEADER
SERVER_EMAIL
SESSION_COOKIE_HTTPONLY
SESSION_COOKIE_SAMESITE
SESSION_COOKIE_SECURE
SESSION_ENGINE
SOCIALACCOUNT_CONNECT_REDIRECT_URL
SOCIALACCOUNT_LOGIN_ON_GET
SOCIALACCOUNT_PROVIDERS
SOCIALACCOUNT_STORE_TOKENS
STATIC_ROOT
STATIC_URL
TEMPLATES
TIME_ZONE
USE_I18N
USE_TZ
WSGI_APPLICATION
X_FRAME_OPTIONS
_DjangoHandler

# ── django_models ── Django ORM model fields & TextChoices members — accessed via `instance.field` / `Class.MEMBER` from Django/templates ──
ACADEMIC_RESEARCHER
ASSISTANT_PROFESSOR
ASSOCIATE_PROFESSOR
GTE_50
H_10_20
H_20_50
H_5_10
INDUSTRY_RESEARCHER
LT_5
Meta
OTHER
PAPER
PAPER_CODE
PHD_STUDENT
POSTDOC
PROFESSOR
constraints
indexes
ordering
review_type
reviewer
reviewer_email
reviewer_first_name
reviewer_h_index
reviewer_last_name
reviewer_role
reviewer_role_other

# ── django_ninja ── Django Ninja route handlers — registered via `@router.<verb>` decorator, never called by name ──
_capture_traceback
_is_pipeline_session_alive
_require_auth_async
ability_not_found
claude_accounts
claude_credentials
claude_usage
delete_file
delete_run
delete_staged_file
delete_staging
download_file
full_message_get
get_cache_key
get_config_presets
list_run_files
list_staged_files
metadata_node_stats_playback
metadata_node_stats_stream
metadata_node_status_playback
metadata_node_status_stream
metadata_trace_playback
metadata_trace_stream
save_config
slim_message_playback
slim_message_stream
start_run
stop_run
stream_runs_status
submit_review
upload_file
upload_staged_file

# ── django_admin ── Django admin registration — `@admin.register` decorator dispatch ──
RunAdmin
list_display
raw_id_fields
search_fields

# ── django_apps ── Django AppConfig — referenced via INSTALLED_APPS string path ──
AbilitiesConfig
DashboardConfig
default_auto_field
verbose_name

# ── django_middleware ── Django middleware — referenced via MIDDLEWARE setting string path ──
DevAutologinMiddleware
RequestLogger

# ── django_entry ── Django entry points (urls/asgi) — Django imports via ROOT_URLCONF / ASGI_APPLICATION setting ──
application
handler405
urlpatterns

# ── django_migration ── Django migration — Django migration runner reads via app introspection ──
operations

# ── opentelemetry ── OpenTelemetry SpanProcessor / MetricExporter protocol overrides — called by SDK, not by name ──
_TOOL_CALL_TYPES
_TOOL_RESULT_TYPES
export
force_flush

# ── fastapi_route ── FastAPI server endpoints — registered via decorator, never called by name ──
_stream
fork_run
get_run_events
get_runs_list

# ── dbos_workflow ── DBOS workflows — registered via @DBOS.workflow decorator, invoked through workflow runtime ──
deploy_gh_workflow
gen_art_demo_workflow
gen_art_workflow
gen_full_paper_workflow
gen_hypo_workflow
gen_paper_text_workflow
gen_plan_workflow
gen_repo_workflow
gen_strat_workflow
gen_viz_workflow
review_hypo_workflow
review_paper_workflow
upd_hypo_workflow

# ── dbos_step ── DBOS steps — registered via @DBOS.step decorator, invoked from inside workflow bodies ──
generate_title_step
write_bytes_durable
write_text_durable

# ── public_api ── module-public helpers re-exported via __all__ for direct import (run-lifecycle emits, active-session helpers) ──
rebuild_nodes
run_end
run_start
wait_until_idle

# ── pydantic_validator ── Pydantic @field_validator / @model_validator hooks — called by Pydantic during model validation ──
_atomic_defaults
_build_api_keys
_enforce_single_task
validate_wikipedia_url

# ── llm_protocol ── LLM client protocol method overrides — called by base-class dispatch ──
create_message
extract_reasoning_from_response
extract_thinking_from_response
extract_usage_from_response

# ── pydantic_hook ── Pydantic v2 framework hook — called by Pydantic on model instantiation ──
model_post_init

# ── proven_used ── Has external references in our codebase — dead analyzer missed it (false positive in dead, not in our code) ──
_fmt_usage
PIPELINE_SEQUENCE
READ_OUT
RED
STARTING
TODO_IN
TODO_OUT
_emit
_must_be_utc
_task_name
agent_summary
agent_system_prompt
aii_ability
available_profiles
bare
colorize
complete
fail
for_run
gen_art
gen_demos
gen_viz
get_backend
is_eligible
kg_dir
messages_file
messages_sequenced_file
run_mode
run_output
task_counter
task_dir
unix
user_uploads
wait_for_running

